# Ollama model to use. Options: qwen2.5:3b, gemma2:2b, llama3.2:3b
OLLAMA_MODEL=qwen2.5:3b

# Ollama server URL (default when running locally)
OLLAMA_HOST=http://localhost:11434

# Max conversation turns before the tutor can reveal the answer
MIN_TURNS_BEFORE_ANSWER=3
